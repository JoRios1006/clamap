/*
  const express = require('express');
  const clamap = require('clamap');
  const parsed = clamap(process.argv;
  const app = express();
  const port = parsed.get('PORT') ?? 8080;

  app.get('/', (req, res) => {
  res.send('Hello World!')
  })
  app.listen(port, () => {
  console.log(`Example app listening at http://localhost:${port}`)
  })
*/
